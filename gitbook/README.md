# Powter

The `powter` project mainly includes `routing` and `powter-client` on client side, `powter-server` on server side, which enables router with capability to modify the route to destination. Its mechanism is designed as following,

<p align="center">
  <img src="https://www.lucidchart.com/publicSegments/view/6f5c9e23-24f3-4e44-bda7-6a1d7c9c2e5c/image.png">
</p>


